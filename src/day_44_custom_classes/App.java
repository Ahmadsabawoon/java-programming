package day_44_custom_classes;

public class App {  // so we use App for other class object it means making object class for App

    String name;
    double version;

    public void open() {
        System.out.println("opening " + name + " app - version = " + version);
    }
}
